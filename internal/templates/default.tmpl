{{define "default"}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{.Page.Name}} - {{.Project.Name}}</title>
    <link rel="stylesheet" href="css/styles.css">
</head>
<body class="font-sans text-base text-text bg-background min-h-screen flex flex-col m-0 p-0">
    {{template "header" .}}
    {{template "main" .}}
    {{template "footer" .}}
</body>
</html>
{{end}} 